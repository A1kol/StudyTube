server:
  port: 8080
spring:
  application:
    name: StudyTube
  datasource:
    url: jdbc:postgresql://postgres:5432/studytube
    username: user
    password: password
  jackson:
    deserialization:
      fail-on-unknown-properties: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: 6379
  ai:
    ollama:
      base-url: ${SPRING_AI_OLLAMA_BASE_URL:http://localhost:11434}
      chat:
        options:
          model: llama3.2

app:
  jwt:
    secret: ${JWT_SECRET}
    expiration: ${JWT_EXPIRATION:86400000} # Сначала ищет в системе, если нет — берет 24ч
  ai:
    chat:
      history-limit: 10
      ttl-hours: 1