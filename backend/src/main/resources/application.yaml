spring:
  application:
    name: StudyTube
  datasource:
    url: jdbc:postgresql://localhost:5432/studytube
    username: user
    password: password
  jackson:
    deserialization:
      fail-on-unknown-properties: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: 6379
  ai:
    ollama:
      base-url: ${SPRING_AI_OLLAMA_BASE_URL:http://localhost:11434}
      chat:
        options:
          model: llama3.2

app:
  jwt:
    secret: ${JWT_SECRET:osjdnccjsab21382198ehsacbsi17^%^&%csdjvbsabcvisa}
    expiration: 86400000
  # НОВЫЙ БЛОК НАСТРОЕК
  ai:
    chat:
      history-limit: 10
      ttl-hours: 1